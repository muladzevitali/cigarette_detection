const apiBaseUrl = process.env['REACT_APP_API_URL'];
const restUrl = `${apiBaseUrl}/rest/v1`;
const detectionUrl = `${restUrl}/detect`;
const mediaUrl = `${restUrl}`;


export {detectionUrl}
export {mediaUrl}